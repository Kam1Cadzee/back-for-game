# Migration `20200604002834-word`

This migration has been generated by Vadim Vereketa at 6/4/2020, 12:28:34 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TYPE "StatusMobile" AS ENUM ('WORK', 'DEPRECATED');

DROP INDEX "public"."Word.en"

CREATE TABLE "public"."MobileApp" (
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"id" SERIAL,"status" "StatusMobile" NOT NULL DEFAULT 'WORK',"url" text  NOT NULL ,"version" text  NOT NULL ,
    PRIMARY KEY ("id"))

ALTER TABLE "public"."Translate" ALTER COLUMN "type" SET DEFAULT 'OTHER';

ALTER TABLE "public"."User" ALTER COLUMN "role" SET DEFAULT 'STANDARD';

ALTER TABLE "public"."Word" ADD COLUMN "userId" integer  NOT NULL ,
ALTER COLUMN "type" SET DEFAULT 'OTHER';

CREATE UNIQUE INDEX "AnotherUser_firstname_lastname_key" ON "public"."Word"("en","userId")

ALTER TABLE "public"."Word" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200601010323-without-unique..20200604002834-word
--- datamodel.dml
+++ datamodel.dml
@@ -8,9 +8,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 model User {
   createdAt DateTime @default(now())
@@ -20,8 +20,9 @@
   lastName  String   @default("")
   password  String   @default("")
   entities  Entity[]
   role      Role     @default(STANDARD)
+  Word      Word[]
 }
 model PartOfSpeechDesc {
   id   Int          @default(autoincrement()) @id
@@ -74,14 +75,17 @@
 }
 model Word {
   id        Int          @default(autoincrement()) @id
-  en        String       @unique
+  en        String
   translate Translate[]  @relation(references: [id])
   type      PartOfSpeech @default(OTHER)
   entities  Entity[]
   createdAt DateTime     @default(now())
   updatedAt DateTime     @default(now())
+  User      User         @relation(fields: [userId], references: [id])
+  userId    Int
+  @@unique([en, userId], name: "AnotherUser_firstname_lastname_key")
 }
 model Translate {
   id        Int          @default(autoincrement()) @id
@@ -104,4 +108,17 @@
   sentence String   @unique
   ru       String
   entities Entity[]
 }
+
+model MobileApp {
+  id        Int          @default(autoincrement()) @id
+  createdAt DateTime     @default(now())
+  version   String
+  url       String
+  status    StatusMobile @default(WORK)
+}
+
+enum StatusMobile {
+  WORK
+  DEPRECATED
+}
```


